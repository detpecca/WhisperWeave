{
  "name": "whisperweave",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @whisperweave/web",
    "build": "npm run build --workspace @whisperweave/web",
    "lint": "npm run lint --workspace @whisperweave/web",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
